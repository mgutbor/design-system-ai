---
'@ods-ai/react': patch
---

Amplía la metadata pública de componentes con los campos opcionales `whenToUse`, `whenNotToUse` y `behavior` (guía de decisión "cuándo usar / cuándo no" y comportamiento), consumibles desde el export `@ods-ai/react/metadata`. Cambio aditivo y retrocompatible: los consumidores existentes siguen funcionando sin cambios.
